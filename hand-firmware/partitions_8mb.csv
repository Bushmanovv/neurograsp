# 8 MB flash partition table for the InMoov hand controller.
# Single app slot (no OTA) so we can hand most of the flash to LittleFS for
# the web assets (Three.js + a decimated GLB model in Layer 5).
#
# Name,     Type, SubType,  Offset,    Size,      Flags
nvs,        data, nvs,      0x9000,    0x5000,
otadata,    data, ota,      0xe000,    0x2000,
app0,       app,  ota_0,    0x10000,   0x300000,   # 3.0 MB application
spiffs,     data, spiffs,   0x310000,  0x4E0000,   # ~4.8 MB LittleFS (label MUST be "spiffs")
coredump,   data, coredump, 0x7F0000,  0x10000,
